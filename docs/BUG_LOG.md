# Bug Log — KV_AI_MT5_EA

Mọi lỗi phát hiện trong quá trình compile/test/review phải ghi lại đây, kể cả khi đã fix ngay, để giữ lịch sử. Không xoá dòng cũ.

| # | Ngày | Milestone | Mô tả | Mức độ | Nguyên nhân gốc | Cách khắc phục | Trạng thái |
|---|---|---|---|---|---|---|---|
| BUG-000 | 2026-08-16 | M0 | Chỉ báo "Davit Pivot" gốc chưa có sẵn (chủ dự án dùng mobile, chưa gửi được file) | Known limitation (không phải bug code) | Thiếu tài liệu nguồn | Dùng placeholder Pivot Classic/Fibonacci, tách interface `CalcPivot()` để thay sau (xem spec §12.1) | OPEN — chờ chủ dự án gửi file/định nghĩa Davit Pivot thật |
| BUG-001 | 2026-08-16 | M0 | Container agent không có MetaEditor/MetaTrader/Wine, không tải được mt5setup.exe qua proxy hiện tại (403) | Môi trường | Sandbox không có MT5 | Biên dịch chuyển sang CI Windows runner (GitHub Actions) | RESOLVED — pipeline chuyển sang CI (BUG-002 là vấn đề mới phát sinh) |
| BUG-002 | 2026-08-16 | M0 | Cả 2 job của workflow `kv-ai-mt5.yml` (chạy `windows-latest` VÀ `ubuntu-latest`) fail ngay trong ~4 giây, `runner_id=0`, không có runner nào được cấp phát, log tải về lỗi HTTP 404 (log không tồn tại vì job chưa từng chạy dòng lệnh nào — kể cả job Ubuntu/pytest đơn giản không liên quan MT5). Chi tiết: run `31922348133`, `https://github.com/DKZ-VN/TradingAgents/actions/runs/31922348133` | Blocker hạ tầng CI | Cả 2 job fail giống hệt nhau kể cả job không đụng MT5/Windows → không phải lỗi cú pháp YAML hay lỗi cài MT5, mà là GitHub Actions bị chặn ở mức tài khoản/repo trước khi cấp runner (dấu hiệu điển hình: hết spending limit $0 cho Actions, hoặc Actions bị tắt cho repo/tài khoản) | Cần chủ dự án (chủ tài khoản GitHub `DKZ-VN`) kiểm tra Settings → Actions → General (Actions có bị "Disable" không) và Settings → Billing → Plans and usage → spending limit cho Actions | **BLOCKED — cần chủ dự án xử lý phía GitHub, agent không có quyền chỉnh billing/permission** |
